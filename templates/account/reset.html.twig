{% extends 'main.html.twig' %}

{% block title %}Восстановление пароля{% endblock %}

{% block content %}

    <div class="alert alert-warning alert-lg">
        <span class="font-weight-bold">ВАЖНО</span>! Компания "Аладдин Р.Д." хранит пароли в безопасном виде и не высылает их своим пользователям.
        Для того, чтобы восстановить доступ к сайту, вам необходимо сменить старый пароль на новый.
    </div>

    <p>Введите адрес E-Mail, на который зарегистрирована ваша учётная запись, чтобы сменить пароль:</p>

    <div class="mt-5">

        {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}

        <div class="form-group row">
            {{ form_label(form.email, 'E-Mail', {'label_attr': {'class': 'col-sm-3 col-form-label text-right'}}) }}
            <div class="col-sm-6">
                {{ form_widget(form.email, {'attr': {'placeholder': 'sergey.ivanov@example.com', 'autofocus': true}}) }}
                {{ form_errors(form.email) }}
            </div>
        </div>

        <div class="form-group row">
            <div class="offset-sm-3 col-sm-9">
                <button type="submit" class="btn btn-primary">Сбросить пароль</button>
            </div>
        </div>

        {{ form_end(form) }}

    </div>

{% endblock %}

{% block sidebar %}
    {{ include('account/sidebar.html.twig') }}
{% endblock %}